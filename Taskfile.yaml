---
version: '3'

set: [pipefail]
shopt: [globstar]

vars:
  BOOTSTRAP_DIR: '{{.ROOT_DIR}}/bootstrap'
  KUBERNETES_DIR: '{{.ROOT_DIR}}/kubernetes'
  SCRIPTS_DIR: '{{.ROOT_DIR}}/scripts'
  TALOS_DIR: '{{.ROOT_DIR}}/talos'
  PRIVATE_DIR: '{{.ROOT_DIR}}/.private'
  TALOSCONFIG: '{{.ROOT_DIR}}/talos/clusterconfig/talosconfig'

env:
  KUBECONFIG: '{{.ROOT_DIR}}/kubeconfig'
  SOPS_AGE_KEY_FILE: '{{.ROOT_DIR}}/age.key'
  TALOSCONFIG: '{{.TALOSCONFIG}}'

includes:
  bootstrap: .taskfiles/bootstrap
  talos: .taskfiles/talos

tasks:

  default: task --list

  reconcile:
    desc: Force Flux to pull in changes from your Git repository
    cmd: flux --namespace flux-system reconcile kustomization flux-system --with-source
    preconditions:
      - test -f {{.KUBECONFIG}}
      - which flux

  verify-oci-digests:
    desc: Verify OCIRepository spec.ref.digest values against registry manifest digests
    cmd: "{{.SCRIPTS_DIR}}/verify-oci-digests.sh {{.ROOT_DIR}}"
    preconditions:
      - which curl
      - which jq

  update-oci-digests:
    desc: Update OCIRepository spec.ref.digest values from registry manifest digests
    cmd: "{{.SCRIPTS_DIR}}/update-oci-digests.sh {{.ROOT_DIR}}"
    preconditions:
      - which curl
      - which jq

  kyverno-test:
    desc: Run Kyverno CLI policy regression tests
    cmd: "{{.SCRIPTS_DIR}}/run-kyverno-cli-tests.sh {{.ROOT_DIR}}"
    preconditions:
      - which docker

  kyverno-chainsaw:
    desc: Run Chainsaw admission tests in a disposable KinD cluster
    cmd: "{{.SCRIPTS_DIR}}/run-kyverno-chainsaw.sh {{.ROOT_DIR}}"
    preconditions:
      - which curl
      - which docker
      - which go
