---
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
resources:
  - rbac.yaml
  # NOTE: CronJob intentionally disabled (kept in repo for reference).
  # Re-enable by adding `cronjob.yaml` back to this list.
