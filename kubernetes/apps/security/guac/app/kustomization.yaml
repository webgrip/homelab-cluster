---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./helmrepository.yaml
  - ./helmrelease.yaml
  - ./httproute.yaml
  - ./sample-data-job.yaml
  - ./s3-collector.yaml
# NO components here. The worker-pool placement component was REMOVED 2026-07-18:
# this app's HelmRelease defines its own spec.postRenderers, and the component's
# strategic-merge patch replaces that list wholesale (see the component's header
# warning) — it silently dropped the rest-api/visualizer resource patches for
# 3 days. The worker affinity (ADR-0002) now lives inline in helmrelease.yaml's
# postRenderers.
