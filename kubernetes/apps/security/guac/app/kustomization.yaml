---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./helmrepository.yaml
  - ./helmrelease.yaml
  - ./httproute.yaml
  - ./sample-data-job.yaml
  - ./s3-collector.yaml
components:
  # Stateless web/collector tiers → worker pool (ADR-0002). Replaces the legacy
  # per-component nodeSelector nodegroup=fringe (removed). guac-db (CNPG) +
  # guac-nats (STS) already on workers.
  - ../../../../components/placement/worker-pool
