---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - rbac.yaml
  - init-cronjob.yaml
  - config-cronjob.yaml
  - snapshot-cronjob.yaml
  # Dynamic Postgres credentials (ADR-0010): generate the freshrss-db vault_admin password
  # once, seed it into OpenBao KV, and mount it into the config job (see config-cronjob.yaml).
  - freshrss-vault-admin.externalsecret.yaml
  - freshrss-vault-admin.pushsecret.yaml
configMapGenerator:
  - name: openbao-bootstrap
    files:
      - init.sh
      - store.sh
      - config.sh
      - snapshot.sh
      - upload.sh
      - admins.hcl
      - external-secrets.hcl
      - snapshot.hcl
      - push.hcl
      - config-admin.hcl
      - cosign-signer.hcl
      - cosign-pub-reader.hcl
generatorOptions:
  disableNameSuffixHash: true
