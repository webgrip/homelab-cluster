---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: security
resources:
  # Add ./dependency-track-secret.sops.yaml after encrypting the bootstrap/application secret.
  - cluster.yaml
  - objectstore.yaml
components:
  - ../../../../../components/cnpg-backup
