apiVersion: v1
kind: Service
metadata:
  name: forbidden-lb-service
  namespace: app
  annotations:
    lbipam.cilium.io/ips: "10.0.0.55"
spec:
  type: LoadBalancer
  selector:
    app: demo
  ports:
    - port: 80
      targetPort: 8080
