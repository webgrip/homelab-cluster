---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./helmrelease.yaml
  - ./ocirepository.yaml
components:
  # App tier → worker pool (ADR-0002); was the only unpinned app, drifting to soyos.
  - ../../../../components/placement/worker-pool
