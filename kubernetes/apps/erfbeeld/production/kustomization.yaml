---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: erfbeeld-production
components:
  # sops: the SOPS-decrypted cluster-secrets Secret in-namespace, for this ns's
  # Flux Kustomization postBuild.substituteFrom (${SECRET_DOMAIN}).
  - ../../../components/sops
  - ../../../components/resource-quota
resources:
  - ./namespace.yaml
  - ./ks.yaml
