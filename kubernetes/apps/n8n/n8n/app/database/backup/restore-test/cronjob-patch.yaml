---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cnpg-restore-test
spec:
  # Intentionally disabled for now.
  suspend: true
  # Poll for a newly completed backup and run once per new backup.
  schedule: "*/10 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: restore-test
              env:
                - name: SOURCE_CLUSTER
                  value: n8n-db
                - name: RESTORE_CLUSTER
                  value: n8n-db-restore
                - name: EXPECTED_DATABASE
                  value: n8n
                - name: EXPECTED_ROLE
                  value: n8n
                - name: BARMAN_OBJECT_NAME
                  value: n8n-db-store
