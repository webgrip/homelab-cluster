---
# yaml-language-server: $schema=https://k8s-schemas.bjw-s.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: grafana-operator
spec:
  interval: 1h
  install:
    timeout: 15m
    crds: CreateReplace
    remediation:
      retries: 3
  upgrade:
    timeout: 15m
    cleanupOnFail: true
    crds: CreateReplace
    remediation:
      retries: 3
      remediateLastFailure: true
  rollback:
    timeout: 15m
  chart:
    spec:
      chart: grafana-operator
      version: 5.23.0
      sourceRef:
        kind: HelmRepository
        name: grafana-operator
        namespace: observability
  values:
    # Empty string means all namespaces. Do not use [] here; the chart renders
    # that as WATCH_NAMESPACE=[] and the operator ignores every real namespace.
    watchNamespaces: ""

    # Use mutable CRDs so Flux handles upgrades without a manual kubectl apply.
    crds:
      immutable: false

    logging:
      level: info
      encoder: console

    resources:
      requests:
        cpu: 10m
        memory: 64Mi
      limits:
        cpu: 100m
        memory: 256Mi

    # Expose operator metrics for Prometheus scraping.
    metrics:
      enabled: true

    serviceMonitor:
      enabled: true
      labels:
        release: kube-prometheus-stack
