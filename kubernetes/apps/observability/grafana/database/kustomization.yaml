---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: observability
resources:
  # grafana-db-secret now sourced from OpenBao via
  # ../app/grafana-db-secret.externalsecret.yaml (rendered by the grafana app
  # Kustomization). Keys: username, password.
  - ../app/database
