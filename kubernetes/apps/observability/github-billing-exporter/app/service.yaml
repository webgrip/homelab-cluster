---
apiVersion: v1
kind: Service
metadata:
  name: github-billing-exporter
  namespace: observability
  labels:
    app.kubernetes.io/name: github-billing-exporter
spec:
  selector:
    app.kubernetes.io/name: github-billing-exporter
  ports:
    - name: http
      port: 8080
      targetPort: http
