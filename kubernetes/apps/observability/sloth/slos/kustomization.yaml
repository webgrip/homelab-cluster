---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./slo-platform-etcd.yaml
  - ./slo-app-availability.yaml
  - ./slo-synthetic-availability.yaml
  # Disabled alongside the k6 suspension (observability/kustomization.yaml, 2026-06-19):
  # this SLO's SLIs come from the k6 canary, which is intentionally off. Re-enable with k6.
  # - ./slo-synthetic-k6-canary.yaml
  - ./slo-garage-availability.yaml
