---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./ocirepository.yaml
  - ./pvc.yaml
  - ./httproute.yaml
  - ./helmrelease.yaml
  # Secrets are created by a human from the *.template.yaml files in this dir.
  # After encrypting with SOPS, uncomment the lines below (see docs/techdocs/docs/forgejo.md):
  # - ./forgejo-admin-secret.sops.yaml
  # - ./forgejo-oidc-secret.sops.yaml
