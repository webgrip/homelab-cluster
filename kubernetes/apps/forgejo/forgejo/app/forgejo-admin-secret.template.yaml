# TEMPLATE — not applied by Flux. Create the real encrypted Secret from this file:
#
#   cd kubernetes/apps/forgejo/forgejo/app
#   cp forgejo-admin-secret.template.yaml forgejo-admin-secret.sops.yaml
#   # edit the values below in the .sops.yaml copy, then:
#   sops --encrypt --in-place forgejo-admin-secret.sops.yaml
#   # finally, uncomment the resource in ./kustomization.yaml and commit.
#
# This is the break-glass LOCAL admin (gitea_admin). Day-to-day login is via Authentik SSO.
# `username` must NOT be "admin" (Forgejo reserves it). Use a strong password.
---
apiVersion: v1
kind: Secret
metadata:
  name: forgejo-admin-secret
  namespace: forgejo
type: Opaque
stringData:
  username: gitea_admin
  password: "REPLACE_WITH_STRONG_PASSWORD"
  email: "admin@webgrip.dev"
