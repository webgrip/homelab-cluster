---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: gitea-mirror
spec:
  chartRef:
    kind: OCIRepository
    name: gitea-mirror
  interval: 1h
  timeout: 10m
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
      strategy: uninstall
  values:
    controllers:
      gitea-mirror:
        annotations:
          reloader.stakater.com/auto: "true"
        strategy: Recreate
        pod:
          # Pin off the soyos onto the worker pool (ADR-0002); native nodeSelector
          # + Recreate (RWO longhorn-general volume → avoids Multi-Attach on the
          # move). Lands on fringe (WFFC-era volume predates worker-1).
          nodeSelector:
            node.webgrip.io/pool: worker
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
            fsGroup: 1000
            fsGroupChangePolicy: OnRootMismatch
        containers:
          app:
            image:
              repository: ghcr.io/raylabshq/gitea-mirror
              tag: v3.20.4@sha256:94b558ffbad75c336df604e206d66d1da8d53e0dea30b762d0d29ead9a10d146
            envFrom:
              - configMapRef:
                  name: gitea-mirror-env
              - secretRef:
                  name: gitea-mirror-secret
            ports:
              - name: http
                containerPort: 4321
            probes:
              liveness:
                enabled: true
              readiness:
                enabled: true
            resources:
              requests:
                cpu: 50m
                memory: 256Mi
              limits:
                # Mirroring large repos (homelab-cluster) + issues/PRs/LFS spikes hard;
                # 512Mi was OOMKilled, so give it real headroom.
                memory: 2Gi
            securityContext:
              allowPrivilegeEscalation: false
              capabilities:
                drop:
                  - "ALL"
    service:
      app:
        ports:
          http:
            port: 4321
            targetPort: http
    persistence:
      data:
        existingClaim: gitea-mirror
        advancedMounts:
          gitea-mirror:
            app:
              - path: /app/data
