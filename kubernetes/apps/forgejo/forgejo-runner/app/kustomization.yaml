---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./configmap.yaml
  - ./triggerauthentication.yaml
  - ./scaledjob.yaml
  # Created by a human from the *.template.yaml files (see docs/techdocs/docs/forgejo.md).
  # After SOPS-encrypting, uncomment:
  # - ./forgejo-runner-secret.sops.yaml
  # - ./forgejo-runner-scaler-token.sops.yaml
