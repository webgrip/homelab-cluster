---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: gha-runner-scale-set-heavy-metrics
  labels:
    app.kubernetes.io/name: gha-runner-scale-set-heavy
spec:
  namespaceSelector:
    matchNames:
      - arc-systems
  podMetricsEndpoints:
    - port: metrics
      path: /metrics
      interval: 30s
      metricRelabelings:
        - targetLabel: pool
          replacement: heavy
  selector:
    matchLabels:
      homelab.webgrip.nl/runner-pool: heavy
