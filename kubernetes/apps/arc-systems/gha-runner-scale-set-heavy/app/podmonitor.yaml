---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: gha-runner-scale-set-metrics
spec:
  namespaceSelector:
    matchNames:
      - arc-systems
  podMetricsEndpoints:
    - targetPort: 8080
      path: /metrics
      interval: 30s
  selector:
    matchLabels:
      homelab.webgrip.nl/runner-pool: heavy
