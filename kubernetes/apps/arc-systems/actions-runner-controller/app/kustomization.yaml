---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./helmrelease.yaml
  - ./ocirepository.yaml
  - ./podmonitor.yaml
components:
  # ARC controller → worker pool (ADR-0002). Stateless Deployment. NOTE: the
  # ephemeral runner pods come from AutoscalingRunnerSet templates, not this HR,
  # so they need their own template nodeSelector (gha-runner-scale-set values).
  - ../../../../components/placement/worker-pool
