---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./general.yaml
  - ./snapshot.yaml
  - ./rwx.yaml
  - ./hdd.yaml
  # ADR-0027 tier scaffolding (inert until the matching Longhorn disk tags exist;
  # the disk-tagging + soyo eviction is a separate supervised step).
  - ./hot.yaml
  - ./cold.yaml
  - ./gitops.yaml
