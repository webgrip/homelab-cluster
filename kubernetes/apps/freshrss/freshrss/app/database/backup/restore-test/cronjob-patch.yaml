---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cnpg-restore-test
spec:
  # Poll for a newly completed backup and run once per new backup.
  schedule: "*/10 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: restore-test
              env:
                - name: SOURCE_CLUSTER
                  value: freshrss-db
                - name: RESTORE_CLUSTER
                  value: freshrss-db-restore
                - name: EXPECTED_DATABASE
                  value: freshrss
                - name: EXPECTED_ROLE
                  value: freshrss
                - name: BARMAN_OBJECT_NAME
                  value: freshrss-db-store
