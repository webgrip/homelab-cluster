---
# Seed the generated grafana_ro credential into OpenBao KV (secret/litellm/grafana-ro) so
# the observability namespace can read the SAME value for the Grafana litellm-db datasource.
# push.hcl already grants write on secret/data/* + secret/metadata/*, no policy change needed.
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: litellm-grafana-ro
spec:
  refreshInterval: 1h
  secretStoreRefs:
    - {kind: ClusterSecretStore, name: openbao-push}
  selector: {secret: {name: litellm-grafana-ro}}
  data:
    - match: {secretKey: username, remoteRef: {remoteKey: litellm/grafana-ro, property: username}}
    - match: {secretKey: password, remoteRef: {remoteKey: litellm/grafana-ro, property: password}}
