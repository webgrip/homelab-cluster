---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./helmrepository.yaml
  - ./helmrelease.yaml
components:
  # KEDA backs the forgejo runners → worker pool (ADR-0002). Operator
  # Deployments, no PVC, so no move-deadlock.
  - ../../../../components/placement/worker-pool
