# Secrets
*.pub
*.key
*.decrypted~*.yaml
.decrypted~*
/age.key
/cloudflare-tunnel.json
/github-deploy.key
/github-deploy.key.pub
/github-push-token.txt
# Template config files
/cluster.toml
# Kubernetes
kubeconfig
talosconfig
# Claude Code (personal/machine-local overrides + secrets)
.claude/settings.local.json
# Misc.
/logs.txt
.private/
.task/
.venv/
.DS_Store
Thumbs.db
**/__pycache__/

# mise machine-local env (secrets/personal — not committed)
.mise.local.toml

# Knowledge-harvest scratch: thread digests + consolidation/truth-check artifacts (transient working files)
harvest-digests/
