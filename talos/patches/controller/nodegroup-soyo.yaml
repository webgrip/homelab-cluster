---
machine:
  nodeLabels:
    # Capability taxonomy (ADR-0001). The legacy `nodegroup` label was retired
    # 2026-06-21 (all apps moved to the pool/cpu/ram scheme). soyos are the
    # low-RAM control-plane pool and are NOT a Longhorn storage node.
    node.webgrip.io/pool: soyo
    node.webgrip.io/cpu: standard
    node.webgrip.io/ram: low
