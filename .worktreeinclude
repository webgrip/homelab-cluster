# Gitignored local files copied into every Claude Code worktree so tooling
# (SOPS decrypt, kubectl, talosctl, mise, validation) works out of the box.
# Syntax = .gitignore. Only files that are ALSO gitignored are copied.
age.key
kubeconfig
.mise.local.toml
.claude/settings.local.json
talos/talosconfig
talos/clusterconfig/
